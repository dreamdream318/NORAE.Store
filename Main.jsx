import React from "react";
import ReactDOM from "react-dom/client";
import NoraeStore from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <NoraeStore />
  </React.StrictMode>
);
